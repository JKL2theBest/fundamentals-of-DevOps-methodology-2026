apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  type: NodePort # Позволяет достучаться до сервиса снаружи кластера
  selector:
    app: my-flask
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30007 # Фиксируем порт на хосте
